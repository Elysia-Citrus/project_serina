# Project Serina — 简历工作内容

## 项目概述

Project Serina 是一个本地桌面 AI 角色对话系统，提供文本 CLI 和语音 CLI 两种交互方式。系统围绕**可解释性、可回归验证、强输出质量控制**构建，从零实现了完整的人格对话 Agent 基础设施。技术栈：Python 3.12、SQLite、YAML 配置驱动，接入 DeepSeek 大模型及本地 ASR/TTS 引擎。

---

## 个人职责与核心工作

### 1. 系统架构设计

- 从零设计了 8 层分层架构（CLI 适配层 → 命令路由 → 会话编排 → 对话引擎 → LLM 网关 → 回复护栏 → 记忆管理 → 调度器），实现各层职责清晰解耦，文本与语音链路复用同一核心对话管道。
- 定义了模块边界与仓库结构，将 `src/`（源码）、`tests/`（测试）、`docs/`（27 篇设计文档）、`evals/`（评测用例）纳入统一维护边界，确保项目可长期迭代。

### 2. 对话引擎与 Prompt 工程

- 实现了完整的回复生成主链路：场景推断 → 记忆检索 → Prompt 组装 → LLM 调用 → 后处理清洗 → 回复护栏评估。
- 设计了基于关键词规则的场景推理系统（问候/闲聊/安慰/深度讨论/纠正），在记忆检索**之前**完成场景判断，影响后续 Prompt 风格与回复长度策略。
- 构建了分层 Prompt 组装机制（身份设定 → 人格配置 → 策略规则 → 场景适配 → 记忆注入 → 时间上下文），确保 LLM 在不同场景下输出风格一致、可控。

### 3. Reply Guard — 输出质量护栏系统（v0.2 核心工作）

- 设计并实现了可解释、可回归的输出质量门控系统，作为 LLM 生成与最终用户输出之间**不可绕过的硬约束层**。
- 建立了稳定的违规分类体系（Taxonomy），覆盖 6 类检测项：AI 自我身份声明、虚构记忆声称、场景冲突、超长输出、模板化客服语气、禁用风格。
- 实现了多级处置策略：`accept`（放行）→ `rewrite`（规则修复）→ `retry_once`（辅助 LLM 重写或低温度重试）→ `safe_fallback`（人格化安全兜底，保留角色人设而非返回系统错误文案）。
- 引入 `initial_action` / `final_action` 分裂记录，使每次护栏决策可追溯、可审计。
- 针对边界场景建立 False Positive 单独看护路径，防止护栏过度触发影响正常对话体验。

### 4. 记忆系统设计

- 设计了四种记忆类型的分层模型：个人档案（低频更新、长生命周期）、事件记忆（7 天活跃窗口、14 天衰减）、会话记忆（跨会话摘要）、续接记录（跨会话桥接）。
- 实现了**高信号写入、受限读取**的记忆策略——通过规则驱动的候选提取与过滤，只持久化有长期价值的用户偏好、任务承诺、重要事件，避免信息囤积。
- 基于 SQLite 实现了记忆持久化与自动列迁移，支持 TTL 过期、去重合并、跨会话可见性控制。
- 提供 `/memory` CLI 命令支持记忆手动审查与覆写，兼顾自动化与用户控制权。

### 5. Assist-LLM 受控辅助通道

- 设计了预算严格、任务白名单化的辅助 LLM 通道，区别于自由思考的 Agent 模式。
- 实施多层安全约束：运行时每轮最多 1 次调用、4 秒超时、输出必须再次通过 Reply Guard、禁止递归调用。
- 定义了 6 个白名单任务（运行时辅助重写、记忆引用校验、Trace 摘要、Badcase 用例生成等），确保辅助 LLM 的输出始终为建议性而非决策性。

### 6. 语音交互子系统

- 集成了本地 ASR（sherpa-onnx SenseVoice）与 TTS（CosyVoice / GPT-SoVITS），实现端到端语音对话。
- 实现了基于 RMS 的语音端点检测、流式 TTS 播放、键盘/麦克风中段打断、自动监听循环等交互功能。
- 语音链路与文本链路完全复用同一套对话引擎、记忆管理和 Reply Guard，唯一的差异仅在于 I/O 管道。

### 7. 测试与评测基础设施

- 建立三层测试体系：Unittest 冒烟测试（快速完整性检查）→ Pytest 模块测试（各子系统单元测试）→ Pytest 回归测试（参数化 case 数据驱动的回归验证）。
- 设计并实现了独立的评测框架（Eval Runner），支持 Mock 模式，覆盖回复护栏、记忆写入/检索、对话流程、False Positive 看护等关键路径。
- 提供 `/trace` 和 `/badcase` 开发调试命令，支持对话 Trace 查看与回归测试用例的半自动生成，形成"发现 badcase → 生成用例 → 回归验证"的闭环。

### 8. 可观测性

- 设计了结构化的 Turn Trace 对象，记录每轮对话的场景、延迟、护栏决策、辅助 LLM 使用情况等关键指标。
- 建立了贯穿全系统的结构化日志规范（`event_name` + 结构化字段），支持 JSONL 持久化与离线分析。

---

## 技术关键词

Python · SQLite · LLM 应用工程 · Prompt Engineering · 对话系统架构 · 输出质量控制 · 记忆系统 · 回归测试 · 语音交互 · ASR/TTS 集成 · 可观测性 · YAML 配置驱动
